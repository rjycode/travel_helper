# 旅游出行助手 · 项目启动指南

> 项目：travel-data（旅游平台业务数据 + 智能客服 + 前端页面）
> 根目录：`D:\python\pythonbase\尚硅谷大模型项目实战之电商小二实战\travel-data`

---

## 0. 系统组成

| # | 服务 | 端口 | 说明 |
|---|------|------|------|
| 1 | travel-data 后端 | **18082** | FastAPI：数据 API + 数据演示台（`/` 页面、`/docs` Swagger） |
| 2 | 智能客服 | **18083** | atguigu：对话路由分发（查机票/查酒店/查订单） |
| 3 | 前端页面 | **5174** | Vite：旅游出行助手机器人页面 |

前置依赖：**MySQL 8**（本机 3305 端口，Windows 服务 `MySQL80`，root/123456）+ Python 3.12 + uv + Node.js

---

## 1. 首次准备（只需一次）

```bash
# 进入项目根目录
cd "D:\python\pythonbase\尚硅谷大模型项目实战之电商小二实战\travel-data"

# 1) 安装 Python 依赖（生成 .venv）
uv sync

# 2) 初始化数据库（建库建表，43 张表）
uv run init_db.py

# 3) 生成业务数据（10 万用户 / 50 万订单，约 10 分钟；小数据量用 --profile smoke）
uv run -m generate.main --profile full
```

> 数据库配置在 `.env`（本机 MySQL：`DB_HOST=127.0.0.1 DB_PORT=3305 DB_USER=root DB_PASSWORD=123456 DB_NAME=travel`）。
> 智能客服会话库：`atguigu_chat.dialogue_states`（已自动建好）。

---

## 2. 日常启动（3 个终端窗口）

### 终端 1：travel-data 后端（18082）

```bash
cd "D:\python\pythonbase\尚硅谷大模型项目实战之电商小二实战\travel-data"
PYTHONPATH= uv run -m app.main
```

验证：浏览器打开 http://127.0.0.1:18082 （数据演示台）或 http://127.0.0.1:18082/docs （Swagger API 文档）

### 终端 2：智能客服（18083）

```bash
cd "D:\python\pythonbase\尚硅谷大模型项目实战之电商小二实战\travel-data\atguigu"
PYTHONPATH=.. uv run python main.py
```

验证：curl http://127.0.0.1:18083/ 返回 `{"success":"ok"}`

### 终端 3：前端页面（5174）

```bash
cd "D:\python\pythonbase\尚硅谷大模型项目实战之电商小二实战\travel-data\customer-service-frontend"
npm run dev
```

验证：浏览器打开 http://127.0.0.1:5174 （旅游出行助手，数字人"小旅"）

---

## 3. 启动后自检

```bash
curl http://127.0.0.1:18082/health          # travel-data → {"status":"ok"}
curl http://127.0.0.1:18083/                # 客服 → {"success":"ok"}
curl http://127.0.0.1:5174/                 # 前端 → 200
```

---

## 4. 停止服务

在每个终端按 `Ctrl + C` 即可。前端 vite 进程在 Windows 上可能残留，可用：

```bash
netstat -ano | findstr :5174        # 找到 PID
taskkill /PID <PID> /F              # 结束进程
```

---

## 5. 常见问题

| 现象 | 原因 | 解决 |
|------|------|------|
| 18082 连不上 | travel-data 没启动 | 启动终端 1 |
| 客服 18083 报错 `No module named 'api'` | 启动目录不对 | 必须在 `atguigu\` 目录下用 `PYTHONPATH=..` 启动 |
| 页面 5174 打不开 | vite 没启动 | 启动终端 3；若端口被占先杀残留进程 |
| 查机票返回"暂无航班" | 该日期恰好没排班 | 换个日期（数据 T-730~T+90 天） |
| 数字人区域"待启用" | 未接入灵眸数字人服务 | 正常现象，文字对话不受影响 |
| `.env` 找不到变量 | 首次配置缺失 | 参考下方 `.env` 模板 |

### .env 关键配置

```ini
# travel-data 数据库（本机 MySQL 3305）
DB_HOST=127.0.0.1
DB_PORT=3305
DB_USER=root
DB_PASSWORD=123456
DB_NAME=travel
APP_PORT=18082

# 智能客服（atguigu）
LLM_MODEL=qwen-plus
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=<你的阿里云百炼 Key>
TRAVEL_API_BASE_URL=http://127.0.0.1:18082
CHAT_DATABASE_URL=mysql+aiomysql://root:123456@127.0.0.1:3305/atguigu_chat?charset=utf8mb4
CHAT_APP_PORT=18083
```

---

## 6. 使用示例（对话）

打开 http://127.0.0.1:5174，在输入框里说：

- "帮我查一下上海到北京的机票" → 依次回答出发/到达城市、日期 → 返回航班和价格
- "查一下上海的酒店" → 依次回答城市、入住/离店日期 → 返回酒店和价格
- "查一下上海的景点" → 依次回答城市、游玩日期 → 返回景点和门票
- "查上海到南京的火车票" → 依次回答出发/到达城市、日期 → 返回车次和价格
- "查上海到苏州的汽车票" → 依次回答出发/到达城市、日期 → 返回班车和票价
- "查一下上海的接送服务" → 依次回答城市、日期 → 返回接机/送机等服务
- "查一下我的订单" → 输入订单号（如 ORD0000000001）→ 返回订单状态和出行信息
- 右侧"机票/酒店"卡片：点"发送机票" → 自动带入城市，只需补日期
